"""
app/shared/components/charts.py
-------------------------
Chart rendering is disabled. Kept as a stub so imports don't break.
Outputs are text + tables only.
"""


def render_chart(data, title: str = "") -> bool:
    """Stub â€” chart rendering is disabled."""
    return False

